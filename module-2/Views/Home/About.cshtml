@{
    ViewBag.Title = "Виконав: Мосейко М.С.";
    ViewBag.Message = "325а гр.";
}
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title</title>
    <!-- Bootstrap CSS -->
    <link href="~/Content/bootstrap.css" rel="stylesheet">
    <style>
        body, html {
            height: 100%;
        }

        body {
            font-family: 'Roboto', sans-serif;
            background: linear-gradient(135deg, #9C9CFF, #ecf96a);
            color: #fff;
        }

        .cover-container {
            height: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
            text-align: center;
        }

        .cover-heading {
            margin-top: 200px;
            font-size: 2rem;
            font-weight: 700;
            color: black;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.5);
        }

        .lead {
            font-size: 1.5rem;
            color: black;
            text-shadow: 1px 1px 2px rgba(0, 0, 0, 0.5);
        }
    </style>
</head>
<body>

    <div class="cover-container">
        <main role="main" class="inner cover" aria-labelledby="title">
            <h1 class="cover-heading" id="title">@ViewBag.Title</h1>
            <h3 class="lead">@ViewBag.Message</h3>
            <p class="lead">Національний аерокосмічний університет імені М. Є. Жуковського "Харківський авіаційний інститут"</p>
        </main>
    </div>
</body>
</html>